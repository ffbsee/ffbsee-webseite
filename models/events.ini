[model]
name = JS-Kalender
label = {{ this.title }}

[fields.title]
label = Title
type = string

[fields.subtitle]
label = Untertitel
type = markdown

[fields.kalender]
label = URL ICS Kalender (Ohne FQDN taucht der Kalender erst online auf)
type = string

[fields.body]
label = Body
type = markdown

